// Package frontend provides the embedded React SPA assets.
//
// Build tag: no_frontend (opt-out when building without the frontend dist)
//
// The real assets are embedded via embed_frontend.go.
// When building without the frontend: go build -tags no_frontend
package frontend
